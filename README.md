# TravelPlus

Flutter project root for the My Trip app. Open this folder in Android Studio:

```text
/home/terry/AndroidStudioProjects/TravelPlus
```

Do not open only `android/`. The Flutter root contains `pubspec.yaml` and `lib/main.dart`.
